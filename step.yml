---
 spec:
   inputs:
     who:
       default: world
---
 exec:
   command:
     - bash
     - -c
     - "echo hello ${{ inputs.who }} && export HELLO=world && touch test123 && echo hello $HELLO && echo test $TEST && ls -la && echo ${{ export_file }}"
